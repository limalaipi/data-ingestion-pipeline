# pip fallback — source of truth is pyproject.toml (use `uv sync`).
polars[rtcompat]>=1.0   # CPU-compat build (no AVX2 required); plain "polars" on modern CPUs
sqlalchemy>=2.0
psycopg2-binary>=2.9
requests>=2.31
pyyaml>=6.0
python-dotenv>=1.0